NODE_ENV=development
PORT=8798
HOST=0.0.0.0
# Local fake URL only — never a production password.
DATABASE_URL=postgres://oneliferelay:oneliferelay@127.0.0.1:5432/oneliferelay
# Comma-separated extra browser origins. Production defaults are in src/config.ts.
CORS_ORIGIN=http://127.0.0.1:4173,http://localhost:4173
